import 'package:flutter/material.dart';

class kits extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Kits",style: TextStyle(color: Colors.teal)),
      ),
    );
  }

}