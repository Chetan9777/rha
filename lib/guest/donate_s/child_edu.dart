import 'package:flutter/material.dart';

class child_edu extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("A child education",style: TextStyle(color: Colors.teal),),
      ),
    );
  }

}